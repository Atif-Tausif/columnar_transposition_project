# CSC3800-Columnar-Transposition-Project

## Run locally

```bash
pip install -r requirements.txt
python app.py
```

## Deploy on Vercel

This project is structured as a Vercel-friendly Flask app. Vercel can detect the
`app` object in `app.py` and install dependencies from `requirements.txt`.
